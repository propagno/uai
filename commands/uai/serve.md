---
name: uai:serve
description: Inicia a interface web de exploração do modelo (porta 3000 por padrão)
argument-hint: "[--port <n>] [--open]"
allowed-tools:
  - Bash
  - Read
---

**Workflow:** `uai/workflows/serve.md`

Execute integralmente esse arquivo. Use `$ARGUMENTS` como foco (porta e opções de inicialização).
